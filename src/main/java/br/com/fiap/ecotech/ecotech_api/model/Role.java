package br.com.fiap.ecotech.ecotech_api.model;

/**
 * Papel do usuário no sistema.
 * USER  -> usuário do app (Flutter)
 * ADMIN -> gestor no painel (Angular), pode gerenciar recompensas
 */
public enum Role {
    USER,
    ADMIN
}
